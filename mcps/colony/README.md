# colony MCP

Source URL: https://github.com/recodeee/colony

Install command:

```sh
npm install -g colony
```

Expected command/type:

```text
command: colony
args: mcp
type: stdio
```

Required env vars:

```text
COLONY_HOME
```

Related skills:

```text
gitguardex
guardex-merge-skills-to-dev
team
trace
worker
```

Quick health check:

```sh
command -v colony
```
