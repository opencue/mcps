# MedusaDocs skills

Related skills from the `soul/skills` registry.

MCP source: [https://docs.medusajs.com/mcp](https://docs.medusajs.com/mcp)

Homepage: [https://docs.medusajs.com](https://docs.medusajs.com)

Install: `hosted endpoint; no local install`

Expected type: `hosted`

Health command: `url:https://docs.medusajs.com/mcp`

Owners: `Medusa`

Agent surfaces: `codex`

| Skill | Category | Reason | Description |
| --- | --- | --- | --- |
| `building-admin-dashboard-customizations` | `medusa` | prefix | Use when user says "Medusa admin", "admin widget", "custom admin page", or "admin form". Required for Medusa Admin widgets, pages, forms, tables, data loading. |
| `building-storefronts` | `medusa` | prefix | Use when user says "Medusa storefront", "call Medusa API", "SDK integration", or "React Query". Required for storefront fetching, mutations, cache, Medusa API calls. |
| `building-with-medusa` | `medusa` | prefix | Use when user says "Medusa backend", "custom module", "API route", "workflow", or "data model". Required for Medusa modules, routes, workflows, links, business logic. |
| `creating-internal-agents` | `medusa` | explicit | Use when user says "Medusa agent", "admin AI agent", "internal agent", or "streamText". Internal admin-facing AI: models, module service, runtime tools, NDJSON routes, admin chat UI. NOT for buyer storefront chatbots. |
| `db-generate` | `medusa` | prefix | Use when user says "medusa db:generate" or "generate migration". Medusa migration generation: model changes, commands, review. |
| `db-migrate` | `medusa` | prefix | Use when user says "medusa db:migrate" or "run migrations". Medusa migration apply: env checks, commands, rollback risk. |
| `gh-submodule-publish` | `medusa` | category | Use when user says "publish submodule" or "Medusa submodule publish". Repo state, commits, push, references, validation. |
| `higgsfield-to-medusa-products` | `medusa` | category | Use when user says "Higgsfield to Medusa", "generate product photos", or "import AI product assets". Asset generation → product mapping → import. |
| `medusa-reference` | `medusa` | explicit | Use when user says "Medusa workflow", "Medusa subscriber", "Medusa auth", "Medusa query", or builds/modifies API routes, models, jobs, backend logic, storefront integrations. |
| `medusa-shop-setup` | `medusa` | category | Use when user says "new Medusa shop", "setup Medusa store", or "Medusa shop scaffold". Base template, backend, storefront, envs, deployment. |
| `new-admin-via-api` | `medusa` | category | Use when user says "create Medusa admin", "new admin via API", or "add admin user". API-based admin creation: auth, request, envs. |
| `new-user` | `medusa` | explicit | Use when user says "new Medusa user", "create first admin", or "add user". CLI/API user creation, credentials. |
| `provision-medusa-s3-bucket` | `medusa` | category | Use when user says "Medusa S3", "provision bucket", or "file storage bucket". S3 provisioning: envs, provider, access. |
| `storefront-best-practices` | `medusa` | explicit | Use when user says "storefront best practices" or "storefront architecture". Data fetching, UX, caching, errors. |
| `woocommerce-to-medusa-import` | `medusa` | explicit | Use when user says "WooCommerce import", "migrate products", or "Woo to Medusa". Extraction, mapping, assets, import. |
